# Contributing

## Branches

- `main` — production-ready
- `develop` — integration
- `feature/*` — one concern per branch (`feature/auth`, `feature/service-requests`)

Do not commit directly to `main`.

## Pull requests

- Clear title and why
- How to test
- No unrelated files

## Commits

Imperative, focused: `Add JWT cookie auth for school users`.
